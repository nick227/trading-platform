# Railway Backend API URL
# This will be set automatically by Railway when both services are deployed
VITE_API_URL=http://localhost:3001

# Note: Alpha Engine communication now goes through secure backend proxy
# No alpha-engine credentials should be exposed to the frontend